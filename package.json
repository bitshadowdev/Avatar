{
  "name": "discordavatar",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "wrangler": "^3.110.0"
  },
  "dependencies": {
    "three": "^0.182.0"
  }
}